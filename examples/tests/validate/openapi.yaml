# Generated with protoc-gen-openapi
# https://github.com/kollalabs/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Messaging API
    version: 0.0.1
paths:
    /v1/messages/{message_id}:
        patch:
            tags:
                - Messaging
            summary: Update Message Summary
            description: This function updates a message.
            operationId: Messaging_UpdateMessage
            parameters:
                - name: message_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: mynum
                  in: query
                  schema:
                    maximum: !!float 30
                    minimum: !!float 1
                    type: integer
                    format: int64
                - name: patterns
                  in: query
                  schema:
                    maxItems: 100
                    minItems: 1
                    type: array
                    items:
                        pattern: ^patterns/[a-z0-9]{26}$
                        type: string
                - name: approval_state
                  in: query
                  description: explicitly include the zero value
                  schema:
                    enum:
                        - APPROVAL_STATE_UNSPECIFIED
                        - APPROVED
                        - REJECTED
                    type: string
                    format: enum
                - name: in_priority
                  in: query
                  schema:
                    enum:
                        - LOW
                        - URGENT
                    type: string
                    format: enum
                - name: not_in_priority
                  in: query
                  schema:
                    enum:
                        - LOW
                        - URGENT
                    type: string
                    format: enum
                - name: const_priority
                  in: query
                  schema:
                    enum:
                        - HIGH
                    type: string
                    format: enum
                - name: offset
                  in: query
                  schema:
                    maximum: !!float 5
                    exclusiveMaximum: true
                    minimum: !!float -5
                    exclusiveMinimum: true
                    type: integer
                    format: sint32
                - name: percent
                  in: query
                  schema:
                    maximum: !!float 100
                    type: integer
                    format: uint32
                    minimum: 0
                - name: ratio
                  in: query
                  schema:
                    maximum: 1.5
                    exclusiveMinimum: true
                    type: number
                    format: double
                    minimum: 0
                - name: step
                  in: query
                  schema:
                    maximum: 0.1
                    minimum: 0.1
                    type: number
                    format: float
                - name: negative
                  in: query
                  schema:
                    maximum: !!float -1
                    minimum: !!float -10
                    type: integer
                    format: int32
                - name: outside
                  in: query
                  description: outside the range (below 5 or above 10), which minimum/maximum can't express
                  schema:
                    type: integer
                    format: int32
            requestBody:
                content:
                    application/json:
                        schema:
                            type: string
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Attachment:
            type: object
            properties:
                url:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Message:
            type: object
            properties:
                message_id:
                    type: string
                text:
                    maxLength: 45
                    minLength: 1
                    type: string
                    format: uri
                mynum:
                    maximum: !!float 30
                    minimum: !!float 1
                    type: integer
                    format: int64
                patterns:
                    maxItems: 100
                    minItems: 1
                    type: array
                    items:
                        pattern: ^patterns/[a-z0-9]{26}$
                        type: string
                approval_state:
                    enum:
                        - APPROVAL_STATE_UNSPECIFIED
                        - APPROVED
                        - REJECTED
                    type: string
                    description: explicitly include the zero value
                    format: enum
                in_priority:
                    enum:
                        - LOW
                        - URGENT
                    type: string
                    format: enum
                not_in_priority:
                    enum:
                        - LOW
                        - URGENT
                    type: string
                    format: enum
                const_priority:
                    enum:
                        - HIGH
                    type: string
                    format: enum
                offset:
                    maximum: !!float 5
                    exclusiveMaximum: true
                    minimum: !!float -5
                    exclusiveMinimum: true
                    type: integer
                    format: sint32
                percent:
                    maximum: !!float 100
                    type: integer
                    format: uint32
                    minimum: 0
                ratio:
                    maximum: 1.5
                    exclusiveMinimum: true
                    type: number
                    format: double
                    minimum: 0
                step:
                    maximum: 0.1
                    minimum: 0.1
                    type: number
                    format: float
                negative:
                    maximum: !!float -1
                    minimum: !!float -10
                    type: integer
                    format: int32
                outside:
                    type: integer
                    description: outside the range (below 5 or above 10), which minimum/maximum can't express
                    format: int32
                attachments:
                    minItems: 1
                    type: array
                    items:
                        $ref: '#/components/schemas/Attachment'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Messaging
