# Generated with protoc-gen-openapi
# https://github.com/kollalabs/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Messaging API
    version: 0.0.1
paths:
    /v1/message_ids/{message_id}:
        get:
            tags:
                - Messaging
            summary: GetMessageById
            description: Literal segment contains the field name; only the placeholder may be renamed.
            operationId: Messaging_GetMessageById
            parameters:
                - name: message_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: user_id
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/messages/{message_id}:
        get:
            tags:
                - Messaging
            summary: GetMessage
            operationId: Messaging_GetMessage
            parameters:
                - name: message_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: user_id
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - Messaging
            summary: CreateMessage
            operationId: Messaging_CreateMessage
            parameters:
                - name: message_id
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/Message'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/users/{user_id}/files/{message_id}:
        get:
            tags:
                - Messaging
            summary: GetMessageByPath
            description: 'Multi-segment wildcard (issue #69): a single parameter whose value may contain slashes.'
            operationId: Messaging_GetMessageByPath
            parameters:
                - name: user_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: message_id
                  in: path
                  required: true
                  allowReserved: true
                  schema:
                    pattern: .+
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/users/{user_id}/messages/{message_id}:
        get:
            tags:
                - Messaging
            summary: GetUserMessage
            operationId: Messaging_GetUserMessage
            parameters:
                - name: user_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: message_id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/users/{user_id}/threads/{thread}:
        get:
            tags:
                - Messaging
            summary: GetThreadMessage
            description: Simple and named path parameters mixed in one template.
            operationId: Messaging_GetThreadMessage
            parameters:
                - name: user_id
                  in: path
                  required: true
                  schema:
                    type: string
                - name: thread
                  in: path
                  description: The thread id.
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v2/users/{user}/messages/{message}:
        get:
            tags:
                - Messaging
            summary: GetNamedMessage
            description: Multiple named path parameters in one template.
            operationId: Messaging_GetNamedMessage
            parameters:
                - name: user
                  in: path
                  description: The user id.
                  required: true
                  schema:
                    type: string
                - name: message
                  in: path
                  description: The message id.
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Message'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Message:
            type: object
            properties:
                message_id:
                    type: string
                user_id:
                    type: string
                content:
                    type: string
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Messaging
