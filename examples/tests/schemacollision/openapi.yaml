# Generated with protoc-gen-openapi
# https://github.com/kollalabs/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Contacts API
    version: 0.0.1
paths:
    /v1/contacts/{contact_id}:
        get:
            tags:
                - Contacts
            summary: GetContact
            operationId: Contacts_GetContact
            parameters:
                - name: contact_id
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Contact'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/google.rpc.Status'
components:
    schemas:
        Address:
            type: object
            properties:
                city:
                    type: string
            description: Declared before Contact, so it's only required after the imported Address was already visited.
        Contact:
            type: object
            properties:
                email:
                    type: string
                external:
                    $ref: '#/components/schemas/tests.schemacollision.other.v1.Contact'
                address:
                    $ref: '#/components/schemas/Address'
                inner:
                    $ref: '#/components/schemas/Middle_Inner'
                other_inner:
                    $ref: '#/components/schemas/tests.schemacollision.other.v1.Outer_Middle_Inner'
                status:
                    $ref: '#/components/schemas/Status'
                phone:
                    $ref: '#/components/schemas/Phone'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Middle_Inner:
            type: object
            properties:
                value:
                    type: string
        Phone:
            type: object
            properties:
                number:
                    type: string
        Status:
            type: object
            properties:
                state:
                    type: string
        google.rpc.Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        tests.schemacollision.other.v1.Contact:
            type: object
            properties:
                url:
                    type: string
            description: Same name as the generated package's Contact; referenced, so it must be package-qualified.
        tests.schemacollision.other.v1.Outer_Middle_Inner:
            type: object
            properties:
                other_value:
                    type: string
            description: Would be named Middle_Inner if only the direct parent were used.
tags:
    - name: Contacts
